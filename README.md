# poker-chips

Play poker with friends at a table without chips

Current - set up to run lambda. 
Running locally, get aws creds and run the local script to emulate the API gateway, then open the HTML file and if all is well it will work.